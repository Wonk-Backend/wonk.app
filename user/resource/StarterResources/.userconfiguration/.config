//Standard settings that /server/ uses for user paths that include 2FA, 3FA, other authentication methods, and backup codes. These paths are also used for user configuration, and user services, and some settings transfer into outside controlled settings.

PATH='user/standard/'
RANK='1'
